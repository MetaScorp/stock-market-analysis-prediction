# Stock Market Analysis and Prediction

Rewriting this old project (originally from 2016, dug up again for a
college assignment in 2021) into something that actually still runs.

The old code is in `legacy/` untouched. The new version lives in `src/`
and is being built from scratch — data access, indicators, risk,
modeling, backtesting, the works. More to come.

See `legacy/README.md` for why the old one stopped working.

# Legacy version

This is the original project, untouched. Started in 2016, reused for a
college assignment in 2021. It doesn't run anymore since the Google
Finance backend it relied on is dead.

Kept here for reference while the rewrite happens in `src/`.
